<h1>Gewinnspiel App</h1>
<% if current_user %>
  <p>
  Du bist <%= current_user.name %> 
  <%= "(Admin)" if current_user.admin? %>
  <%= "(Bar)" if current_user.bar? %>
  <%= link_to 'Logout', user_path(current_user), data: { turbo_method: :delete } %>
  </p>
<% end %>
<% if current_participant %>
  <p>
  Hallo <%= current_participant.name %>, du nimmst am Gewinnspiel teil! <%= link_to 'Teilnahme-QR-Code', participant_path(current_participant.uid) %>
  </p>
<% end %>
<ul class="classic-ul">
  <li><%= link_to 'Teilnehmer-Liste', participants_path %></li>
  <li>
    <%= link_to 'Teilnehmer-Registration', new_participant_path %>
  </li>
  <li>
    <%= link_to 'Einladungen', invitations_path %>
  </li>
</ul>
<h2>Teilnehmer Registration</h2>
<p>Entweder via Direktlink: <%= link_to 'Teilnehmer-Registration', new_participant_path %>
</p>
<p>Oder via QR code: <%= image_tag RQRCode::QRCode.new(new_participant_url).as_png(size: 500).to_data_url %> </p>
